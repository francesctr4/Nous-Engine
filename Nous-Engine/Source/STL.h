#pragma once

#include <string>			// Bombardeen 
#include <vector>			// Dynamic array
#include <array>			// Fixed-size array
#include <deque>			// Double-ended queue
#include <list>				// Doubly-linked list
#include <forward_list>		// Singly-linked list
#include <map>				// Sorted key-value pairs
#include <unordered_map>	// Unsorted key-value pairs (hash table)
#include <set>				// Sorted unique elements
#include <unordered_set>	// Unsorted unique elements (hash table)
#include <stack>			// LIFO data structure (usually backed by deque or vector)
#include <queue>			// FIFO data structure (usually backed by deque)